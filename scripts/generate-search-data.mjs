
// This script is intended to be run at build time to create a static search index.
// It is no longer needed as we are using a manually managed public/search-data.json file.
// The file is kept to avoid breaking the build process if it's referenced in package.json,
// but it performs no action.
console.log("Using static search data from /public/search-data.json. Generate script is disabled.");

    